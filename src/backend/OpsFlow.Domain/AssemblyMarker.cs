namespace OpsFlow.Domain;

/// <summary>
/// Identifies the OpsFlow.Domain assembly for tests and reflection.
/// This type carries no business logic; it exists only as a stable anchor
/// so other assemblies can reference the Domain assembly without depending
/// on a concrete business type.
/// </summary>
public sealed class AssemblyMarker
{
}
